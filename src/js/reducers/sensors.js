import * as ActionTypes from '../actions/sensors';
import merge from 'lodash.merge';

const profile = (state = {
}, action) => {
  const {type} = action;
  switch (type) {
  }

  return state;
};


export default profile;
